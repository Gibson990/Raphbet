on small devices i see no USDT logo
my bets,profile and  wallet the padding /margin in not good the widgets /components touch the edge
style well the top nav as the price display widget is a bit big and uses defualf icon

when login user is not sent to home page
if user login send them to home page
we need proper state managemnet ana rooting 

the side is dragable like there is extra space  

on mobile when user select  more market and goals the given sheet doen not give user time to access as if they select it just adds to bet slip

last we need to plan for our support feature 

handle imput,empty states ,progress bars sucess screens ,fail screens 

also the black test fonts still diaplay black in dark mode 

make sure user can edit profile image and user name though the firts deafulte user name and profile image should be based on
signup is siginup with gmail 
if not we can request them to add user name and profile image 

let me know the KYC process only works if the real auth in complete?



sokonicity:


Local is 100% launch-ready. I've saved the full go-live runbook to memory, so next session we go straight to:
1. Deploy — backend → Railway, frontend → Vercel
2. Migrate data → prod Mongo (catalog + re-run the gitignored migrations + the collections seed)
3. Payments live → AzamPay production creds + real callback URLs
4. Config → NODE_ENV=production, rotate JWT secret, fix the Vercel \r\n
5. Smoke test → launch 🚀
